# Your server's LAN IP address — the IP other devices will use to reach this proxy.
# Find it with: ip route get 1 | awk '{print $7; exit}'  (Linux)
#           or: ipconfig getifaddr en0                    (macOS)
LOCAL_IP=10.0.0.43